extends layout

block content
  .container
    header
      h1 Feeds
    .content
      div.feedlist.list-group
        each feed in feeds
          a.list-group-item(href='feeds/' + feed) 
            span.item-text #{feed}